server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/Users/alexg/Documents/config-repo
  profiles:
    active: native

management:
  endpoints:
    web:
      exposure:
        include: health,info